create table stat_ybi(
    year varchar(4),
    bra_id varchar(9),
    cnae_id varchar(6),
    wage float,
    num_jobs int,
    num_est int,
    wage_avg float,
    rca float,
    distance float,
    opp_gain float
);